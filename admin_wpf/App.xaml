<Application x:Class="ShetMitraAdmin.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:ShetMitraAdmin.Converters"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <conv:MismatchToBrushConverter x:Key="MismatchToBrushConverter" />
            <conv:PricePressureToBrushConverter x:Key="PricePressureToBrushConverter" />
            <conv:VarietyStatusToBrushConverter x:Key="VarietyStatusToBrushConverter" />
            <conv:BearingToBrushConverter x:Key="BearingToBrushConverter" />
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

            <SolidColorBrush x:Key="BrandPrimary" Color="#2E7D32" />
            <SolidColorBrush x:Key="BrandAccent" Color="#FFB300" />
            <SolidColorBrush x:Key="SurfaceCard" Color="#FFFFFF" />
            <SolidColorBrush x:Key="SurfaceBg" Color="#F5F7F4" />
            <SolidColorBrush x:Key="TextPrimary" Color="#1B1B1B" />
            <SolidColorBrush x:Key="TextMuted" Color="#666666" />
            <SolidColorBrush x:Key="Danger" Color="#C62828" />
            <SolidColorBrush x:Key="Warning" Color="#EF6C00" />
            <SolidColorBrush x:Key="Success" Color="#2E7D32" />

            <Style TargetType="TextBlock" x:Key="CardTitle">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
                <Setter Property="FontWeight" Value="SemiBold" />
            </Style>

            <Style TargetType="TextBlock" x:Key="CardValue">
                <Setter Property="FontSize" Value="26" />
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
                <Setter Property="FontWeight" Value="Bold" />
                <Setter Property="Margin" Value="0,4,0,0" />
            </Style>

            <Style TargetType="Border" x:Key="Card">
                <Setter Property="Background" Value="{StaticResource SurfaceCard}" />
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="16" />
                <Setter Property="Margin" Value="8" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>

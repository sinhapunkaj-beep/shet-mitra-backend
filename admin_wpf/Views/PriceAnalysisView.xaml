<UserControl x:Class="ShetMitraAdmin.Views.PriceAnalysisView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ShetMitraAdmin.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             d:DataContext="{d:DesignInstance Type=vm:PriceAnalysisViewModel, IsDesignTimeCreatable=True}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{StaticResource SurfaceBg}">
    <UserControl.DataContext>
        <vm:PriceAnalysisViewModel />
    </UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="16">
        <StackPanel>
            <TextBlock Text="Price Analysis"
                       FontSize="26"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimary}"
                       Margin="8,8,0,16" />

            <!-- Filter row: Commodity / Variety (Mango only) / Mandi -->
            <Border Style="{StaticResource Card}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,12,0">
                        <TextBlock Text="Commodity" Style="{StaticResource CardTitle}" />
                        <ComboBox ItemsSource="{Binding AvailableCommodities}"
                                  SelectedItem="{Binding SelectedCommodity, Mode=TwoWay}"
                                  Margin="0,4,0,0"
                                  Padding="6,4" />
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Margin="0,0,12,0"
                                Visibility="{Binding IsMangoSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="Variety" Style="{StaticResource CardTitle}" />
                        <ComboBox ItemsSource="{Binding AvailableVarieties}"
                                  SelectedItem="{Binding SelectedVariety, Mode=TwoWay}"
                                  Margin="0,4,0,0"
                                  Padding="6,4" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Mandi" Style="{StaticResource CardTitle}" />
                        <ComboBox ItemsSource="{Binding AvailableMandis}"
                                  SelectedItem="{Binding SelectedMandi, Mode=TwoWay}"
                                  Margin="0,4,0,0"
                                  Padding="6,4" />
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Price chart with ON/OFF bearing-year band overlay -->
            <Border Style="{StaticResource Card}">
                <StackPanel>
                    <DockPanel LastChildFill="False" Margin="0,0,0,8">
                        <TextBlock DockPanel.Dock="Left"
                                   Text="Price history"
                                   FontSize="16"
                                   FontWeight="SemiBold" />
                        <StackPanel DockPanel.Dock="Right"
                                    Orientation="Horizontal"
                                    Visibility="{Binding IsMangoSelected, Converter={StaticResource BooleanToVisibilityConverter}}">
                            <Border CornerRadius="4"
                                    Padding="8,3"
                                    Margin="0,0,6,0">
                                <Border.Background>
                                    <Binding Source="ON" Converter="{StaticResource BearingToBrushConverter}" />
                                </Border.Background>
                                <TextBlock Text="ON year"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="#1B1B1B" />
                            </Border>
                            <Border CornerRadius="4"
                                    Padding="8,3">
                                <Border.Background>
                                    <Binding Source="OFF" Converter="{StaticResource BearingToBrushConverter}" />
                                </Border.Background>
                                <TextBlock Text="OFF year"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="#1B1B1B" />
                            </Border>
                        </StackPanel>
                    </DockPanel>

                    <lvc:CartesianChart Height="320"
                                        Series="{Binding PriceChartSeries}"
                                        XAxes="{Binding PriceChartXAxes}"
                                        YAxes="{Binding PriceChartYAxes}"
                                        Sections="{Binding BearingBandsArray}" />

                    <TextBlock Text="{Binding StatusMessage}"
                               Foreground="{StaticResource TextMuted}"
                               FontSize="12"
                               Margin="0,8,0,0" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
